name: 🖥 Build Horizon Thin Client ISO
on:
  workflow_dispatch:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 📥 Checkout code (with LFS)
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: 🛠 Install live-build and tools
        run: |
          sudo apt update
          sudo apt install -y live-build xorriso

      - name: 🔍 Verify lb command exists
        run: |
          which lb || { echo "❌ live-build not installed"; exit 1; }
          lb --version

      - name: 📦 Check Horizon .deb present
        run: |
          ls -lh config/packages.chroot/
          if [ ! -f config/packages.chroot/VMware-Horizon-Client-*.deb ]; then
            echo "❌ ERROR: Horizon .deb missing in config/packages.chroot/"
            echo "💡 Hint: Use Git LFS and ensure 'lfs: true' in checkout"
            exit 1
          fi

      - name: ⚙️ Configure live-build (GRUB only)
        run: |
          # Clean up any existing config to avoid conflicts
          rm -rf config/auto config/package-lists/*.list.binary config/package-lists/*.list.chroot_auto 2>/dev/null || true

          lb config \
            --distribution jammy \
            --architectures amd64 \
            --bootloader grub \
            --syslinux-themes none \
            --archive-areas "main restricted universe multiverse" \
            --bootappend-live "boot=live quiet splash nomodeset"

      - name: 🧪 Configure package lists
        run: |
          # 确保使用我们的包列表
          echo 'LB_PACKAGE_LISTS="horizon"' >> config/binary

          # 创建二进制包列表
          echo "live-boot" > config/package-lists/horizon.list.binary

          # 检查并移除任何包含 "auto" 的包列表
          echo "=== Checking for 'auto' in package lists ==="
          find config/package-lists/ -type f -name "*.list.*" 2>/dev/null | while read file; do
            if grep -q "^auto$" "$file" || grep -q "^auto " "$file"; then
              echo "Removing 'auto' from $file"
              grep -v "^auto$" "$file" | grep -v "^auto " > "${file}.fixed"
              mv "${file}.fixed" "$file"
            fi
          done

          # 创建空包列表以覆盖默认设置
          echo "# Empty package list" > config/package-lists/empty.list.chroot
          echo "# Empty binary package list" > config/package-lists/empty.list.binary
          echo 'LB_PACKAGE_LISTS="horizon empty"' >> config/binary

      - name: 🏗 Build ISO
        run: |
          echo "🚀 Starting build..."
          echo "=== Final package lists ==="
          find config/package-lists/ -type f -name "*.list.*" -exec sh -c 'echo "{}:"; cat {}; echo' \; 2>/dev/null || true
          echo ""
          echo "=== Final config/binary ==="
          cat config/binary
          echo ""
          time sudo lb build

      - name: 📤 Upload ISO
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: horizon-thin-client.iso
          path: live-image-amd64.hybrid.iso

      - name: 📊 Summary
        if: always()
        run: |
          echo "✅ Done at $(date)"
          ls -lh live-image-amd64.hybrid.iso 2>/dev/null || echo "⚠ ISO not found"
