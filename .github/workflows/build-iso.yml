name: 🖥 Build Horizon Thin Client ISO
on:
  workflow_dispatch:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 📥 Checkout code (with LFS)
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: 🛠 Install live-build and fix apt sources
        run: |
          # 完全修复 apt 源配置
          echo "=== Completely fixing apt sources ==="
          # 备份原始 sources.list
          sudo cp /etc/apt/sources.list /etc/apt/sources.list.backup
          # 创建正确的 sources.list
          echo "deb https://archive.ubuntu.com/ubuntu jammy main restricted universe multiverse" > /tmp/sources.list
          echo "deb https://archive.ubuntu.com/ubuntu jammy-updates main restricted universe multiverse" >> /tmp/sources.list
          echo "deb https://security.ubuntu.com/ubuntu jammy-security main restricted universe multiverse" >> /tmp/sources.list
          sudo mv /tmp/sources.list /etc/apt/sources.list

          sudo apt update
          sudo apt install -y live-build xorriso
          lb --version

      - name: 🔍 Verify lb command exists
        run: |
          which lb || { echo "❌ live-build not installed"; exit 1; }
          lb --version

      - name: 📦 Check Horizon .deb present
        run: |
          ls -lh config/packages.chroot/
          if [ ! -f config/packages.chroot/VMware-Horizon-Client-*.deb ]; then
            echo "❌ ERROR: Horizon .deb missing in config/packages.chroot/"
            echo "💡 Hint: Use Git LFS and ensure 'lfs: true' in checkout"
            exit 1
          fi

      - name: ⚙️ Configure live-build
        run: |
          # 清理现有配置
          rm -rf config/auto config/package-lists/*.list.binary config/package-lists/*.list.chroot_auto 2>/dev/null || true

          # 使用最小配置，指定正确的镜像
          lb config \
            --distribution jammy \
            --architectures amd64 \
            --bootloader grub-pc \
            --archive-areas "main restricted universe multiverse" \
            --bootappend-live "boot=live quiet splash nomodeset" \
            --mirror-bootstrap "https://archive.ubuntu.com/ubuntu/" \
            --mirror-chroot "https://archive.ubuntu.com/ubuntu/" \
            --mirror-chroot-security "https://security.ubuntu.com/ubuntu/" \
            --security false

      - name: 🧪 Configure package lists
        run: |
          # 确保包列表目录存在
          mkdir -p config/package-lists

          # 创建二进制包列表
          echo "live-boot" > config/package-lists/horizon.list.binary

          # 创建空包列表以覆盖默认设置
          echo "# Empty package list" > config/package-lists/empty.list.chroot
          echo "# Empty binary package list" > config/package-lists/empty.list.binary

          # 确保 GRUB 包在包列表中（修复 grub-legacy 错误）
          echo "=== Adding GRUB packages to horizon.list.chroot ==="
          if [ -f "config/package-lists/horizon.list.chroot" ]; then
            # 添加正确的 GRUB 包
            echo "grub-pc" >> config/package-lists/horizon.list.chroot
            echo "grub-common" >> config/package-lists/horizon.list.chroot
            echo "grub2-common" >> config/package-lists/horizon.list.chroot
            echo "Added GRUB packages to horizon.list.chroot"
          else
            echo "WARNING: horizon.list.chroot not found"
          fi

          # 修复 chroot 内的 apt 源配置
          echo "=== Fixing apt sources in chroot ==="
          mkdir -p config/archives
          # 创建正确的 sources.list 文件
          echo "deb https://archive.ubuntu.com/ubuntu jammy main restricted universe multiverse" > config/archives/ubuntu.list.chroot
          echo "deb https://archive.ubuntu.com/ubuntu jammy-updates main restricted universe multiverse" >> config/archives/ubuntu.list.chroot
          echo "deb https://security.ubuntu.com/ubuntu jammy-security main restricted universe multiverse" >> config/archives/ubuntu.list.chroot
          # 确保 live-build 使用我们的源文件
          echo 'LB_APT_SOURCE_ARCHIVES="ubuntu"' >> config/binary
          # 禁用 security 仓库的自动检测
          echo 'LB_SECURITY="false"' >> config/binary
          # 禁用 apt 源的自动更新
          echo 'LB_APT_SOURCES_LIST="false"' >> config/binary

          # 设置包列表配置
          echo 'LB_PACKAGE_LISTS="horizon empty"' >> config/binary
          echo 'LB_APT_INDICES="false"' >> config/binary
          echo 'LB_APT_RECOMMENDS="false"' >> config/binary
          echo 'LB_APT_OPTIONS="--allow-unauthenticated"' >> config/binary
          # 指定 GRUB 包，避免使用 grub-legacy
          echo 'LB_GRUB_PACKAGES="grub-pc"' >> config/binary
          echo 'LB_GRUB="grub-pc"' >> config/binary
          echo 'LB_GRUB_INSTALL="grub-install"' >> config/binary
          echo 'LB_GRUB_MKCONFIG="grub-mkconfig"' >> config/binary

      - name: 🧪 Verify config before build
        run: |
          echo "=== Verifying configuration ==="
          echo "config/binary content:"
          cat config/binary
          echo ""
          echo "Package lists:"
          ls -la config/package-lists/

      - name: 🏗 Build ISO
        run: |
          echo "🚀 Starting build..."
          echo "=== Final configuration ==="
          cat config/binary
          echo ""
          echo "=== Checking apt sources in chroot ==="
          cat config/archives/ubuntu.list.chroot || echo "No ubuntu.list.chroot found"
          echo ""
          echo "=== Starting lb build with debugging ==="
          set -x
          time sudo lb build 2>&1 | tee build.log
          set +x
          echo ""
          echo "=== Build log summary ==="
          tail -50 build.log | grep -E "(Error|error|FAIL|fail|auto|Release)" || echo "No significant errors found"

      - name: 📤 Upload ISO
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: horizon-thin-client.iso
          path: live-image-amd64.hybrid.iso

      - name: 📊 Summary
        if: always()
        run: |
          echo "✅ Done at $(date)"
          ls -lh live-image-amd64.hybrid.iso 2>/dev/null || echo "⚠ ISO not found"
