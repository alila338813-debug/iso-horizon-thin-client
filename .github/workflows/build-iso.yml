name: 🖥️ Build Horizon Thin Client ISO
on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 📥 Checkout code (with LFS)
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: 🛠️ Install live-build and tools
        run: |
          sudo apt update
          sudo apt install -y live-build xorriso

      - name: 🔍 Verify lb command exists
        run: |
          which lb || { echo "❌ live-build not installed"; exit 1; }
          lb --version

      - name: 📦 Check Horizon .deb present
        run: |
          ls -lh config/packages.chroot/
          if [ ! -f config/packages.chroot/vmware-horizon-client_*.deb ]; then
            echo "❌ ERROR: Horizon .deb missing in config/packages.chroot/"
            echo "💡 Hint: Use Git LFS and ensure 'lfs: true' in checkout"
            exit 1
          fi

      - name: 🧪 Debug config
        run: |
          echo "=== config/binary ==="
          cat config/binary

      - name: 🏗️ Build ISO
        run: |
          echo "🚀 Starting build..."
          time sudo lb build

      - name: 📤 Upload ISO
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: horizon-thin-client.iso
          path: live-image-amd64.hybrid.iso

      - name: 📊 Summary
        if: always()
        run: |
          echo "✅ Done at $(date)"
          ls -lh live-image-amd64.hybrid.iso 2>/dev/null || echo "⚠️ ISO not found"
